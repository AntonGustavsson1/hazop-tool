__pycache__/
*.pyc
*.db
*.db-shm
*.db-wal
*.pdf
*.xlsx

# Logs from local runs (hazop_crash.log, hazop_debug.log, test_run.log, ...)
*.log

# Local save files used for manual/exploratory testing
*.hzp

# Crash reports (see CLAUDE.md "Crash Reporting") — local diagnostic
# artifacts, not source
crashes/

# Everything that isn't part of the program itself — screenshots, logs,
# large local-only P&ID/reference material (see NOTES.md "Autodetektera
# utrustning..." 2026-08-05: "riktiga P&ID ref/-filer används bara för
# lokal manuell körning, aldrig som testfixturer"), and stale project
# files superseded by the live hazop_project.db (2026-08-20 cleanup)
ej_programfiler/
